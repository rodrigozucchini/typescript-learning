(() => {
  //puedo usar comillas simples o dobles
  let productTitle:string = "Amazing title";
  console.log("productTitle", productTitle);

  let productComment:string = `${productTitle}: es genial`;
  console.log("productComment", productComment);
})();
